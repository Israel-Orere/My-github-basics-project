import './globals.css'; import type {ReactNode} from 'react';
export const metadata={title:'DreamForge — Event Contract Strategy OS',description:'Build, backtest and deploy transparent DreamDEX Event Contract strategies.'};
export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}
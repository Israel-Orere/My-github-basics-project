import {NextResponse} from 'next/server';import {getLiveMarkets} from '@/lib/dreamdex';
export const dynamic='force-dynamic';export async function GET(){const started=Date.now();try{const markets=await getLiveMarkets();return NextResponse.json({ok:true,service:'DreamForge',network:'Somnia Shannon',chainId:50312,liveMarkets:markets.length,latencyMs:Date.now()-started,execution:'wallet-authorized only',syntheticFallback:false});}catch(e){return NextResponse.json({ok:false,service:'DreamForge',network:'Somnia Shannon',chainId:50312,error:e instanceof Error?e.message:'health check failed',syntheticFallback:false},{status:503});}}
